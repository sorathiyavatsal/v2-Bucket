# ============================================
# V2-Bucket Platform - LOCAL DEVELOPMENT
# ============================================
# Copy this file to .env for local development
# Usage: cp .env.local.example .env

# ============================================
# DATABASE (Required)
# ============================================
# For local development, you can use Docker Compose to run PostgreSQL:
# docker-compose -f docker-compose.dev.yml up -d postgres
# Or use a local PostgreSQL installation
POSTGRES_USER=v2bucket
POSTGRES_PASSWORD=dev-password-123
POSTGRES_DB=v2bucket
POSTGRES_PORT=5432

# Full DATABASE_URL (automatically constructed by the app, but you can override)
# DATABASE_URL=postgresql://v2bucket:dev-password-123@localhost:5432/v2bucket?schema=public

# ============================================
# REDIS CACHE (Required)
# ============================================
# For local development, you can use Docker Compose to run Redis:
# docker-compose -f docker-compose.dev.yml up -d redis
REDIS_PASSWORD=dev-redis-password
REDIS_PORT=6379

# Full REDIS_URL (automatically constructed by the app, but you can override)
# REDIS_URL=redis://:dev-redis-password@localhost:6379

# ============================================
# AUTHENTICATION (Required)
# ============================================
# JWT Secret - Generate: openssl rand -base64 32
# For local dev, you can use a simple secret
JWT_SECRET=local-dev-jwt-secret-change-in-production

# Better-Auth Secret - Generate: openssl rand -base64 32
# For local dev, you can use a simple secret
BETTER_AUTH_SECRET=local-dev-better-auth-secret-change-in-production

# Better-Auth URL (your API URL)
BETTER_AUTH_URL=http://localhost:3000

# ============================================
# APPLICATION (Required)
# ============================================
# API Server Port
API_PORT=3000

# Web UI Port
WEB_PORT=3001

# CORS Origin (web UI URL)
CORS_ORIGIN=http://localhost:3001

# ============================================
# NEXT.JS PUBLIC VARIABLES (Required)
# ============================================
# These are exposed to the browser
NEXT_PUBLIC_API_URL=http://localhost:3000

# ============================================
# STORAGE (Optional)
# ============================================
# Local storage path for uploaded files
# STORAGE_PATH=./storage

# ============================================
# DEVELOPMENT NOTES
# ============================================
# 1. This file is for LOCAL DEVELOPMENT only
# 2. NEVER use these values in production
# 3. Run database and redis with: docker-compose -f docker-compose.dev.yml up -d
# 4. Start API: cd apps/api && pnpm dev
# 5. Start Web: cd apps/web && pnpm dev
# 6. Generate strong secrets for production: openssl rand -base64 32
