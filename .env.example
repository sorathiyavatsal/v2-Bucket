# V2-Bucket Environment Configuration

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Local Docker: postgresql://postgres:postgres@localhost:5433/v2bucket
# Production: postgresql://user:password@postgres:5432/v2bucket
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/v2bucket

# =============================================================================
# CACHE CONFIGURATION
# =============================================================================

# Redis connection string
# Local Docker: redis://localhost:6380
# Production: redis://redis:6379
REDIS_URL=redis://localhost:6380

# =============================================================================
# STORAGE CONFIGURATION
# =============================================================================

# Storage path inside Docker container (don't change this)
STORAGE_PATH=/storage

# Storage path on host filesystem (NAS location)
# This is where your uploaded files will be stored on the NAS
# The directory will be created automatically if it doesn't exist
#
# Synology NAS examples:
#   /volume1/docker/v2-bucket/storage
#   /volume1/v2bucket-files
#   /volume2/backups/v2bucket
#
# QNAP NAS examples:
#   /share/Container/v2-bucket/storage
#   /share/Public/v2bucket
#
# For local testing:
#   ./storage (relative to docker-compose.yml)
#   /absolute/path/to/storage
STORAGE_HOST_PATH=./storage

# =============================================================================
# API CONFIGURATION
# =============================================================================

# API server port
PORT=3000

# Node environment (development|production)
NODE_ENV=production

# API URL (used by web dashboard)
# Local: http://localhost:3000
# Production with Tailscale: https://your-hostname.ts.net/api
NEXT_PUBLIC_API_URL=http://localhost:3000

# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================

# Better-Auth secret (32+ random characters)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-super-secret-key-change-in-production-32-chars

# Better-Auth URL (web dashboard URL)
# Local: http://localhost:3001
# Production with Tailscale: https://your-hostname.ts.net
BETTER_AUTH_URL=http://localhost:3001

# JWT secret for S3 API compatibility (32+ random characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-key-change-in-production

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed CORS origins (comma-separated)
# Local: http://localhost:3001
# Production: https://your-domain.com
CORS_ORIGIN=http://localhost:3001

# =============================================================================
# AWS/S3 CONFIGURATION
# =============================================================================

# Default AWS region for S3 API
DEFAULT_REGION=us-east-1

# Default storage class for new objects
DEFAULT_STORAGE_CLASS=STANDARD

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level (debug|info|warn|error)
LOG_LEVEL=info

# =============================================================================
# WEB DASHBOARD CONFIGURATION
# =============================================================================

# Application name
NEXT_PUBLIC_APP_NAME=V2-Bucket Platform

# Application URL
# Local: http://localhost:3001
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3001

# =============================================================================
# TAILSCALE CONFIGURATION (Optional - for public access)
# =============================================================================

# Tailscale authentication key (get from https://login.tailscale.com/admin/settings/keys)
# TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxx

# Tailscale hostname
# TS_HOSTNAME=v2bucket

# =============================================================================
# EMAIL CONFIGURATION (Optional - for notifications)
# =============================================================================

# Resend API key (get from https://resend.com)
# RESEND_API_KEY=your-resend-api-key

# From email address
# RESEND_FROM_EMAIL=V2-Bucket <noreply@yourdomain.com>

# =============================================================================
# GIT REPOSITORY (for Docker deployment from Git)
# =============================================================================

# Repository URL
GIT_REPO=https://github.com/sorathiyavatsal/v2-Bucket.git

# Branch to deploy
GIT_BRANCH=main
