# ============================================
# V2-Bucket Platform - DOCKER / NAS DEPLOYMENT
# ============================================
# Copy this file to .env for Docker deployment on NAS
# Usage: cp .env.docker.example .env

# ============================================
# GIT REPOSITORY (Required for Docker Deployment)
# ============================================
# Your GitHub repository URL - used by docker-compose to pull code
# HTTPS: https://github.com/yourusername/v2-bucket.git
# SSH: git@github.com:yourusername/v2-bucket.git
# Private repo with token: https://TOKEN@github.com/yourusername/v2-bucket.git
GIT_REPO=https://github.com/sorathiyavatsal/v2-Bucket.git
GIT_BRANCH=main

# ============================================
# DATABASE (Required)
# ============================================
POSTGRES_USER=v2bucket
POSTGRES_PASSWORD=v2bucket
POSTGRES_DB=v2bucket
POSTGRES_PORT=5433

# ============================================
# REDIS CACHE (Required)
# ============================================
REDIS_PASSWORD=v2bucket
REDIS_PORT=6380

# ============================================
# AUTHENTICATION (Required)
# ============================================
# JWT Secret - Generate: openssl rand -base64 32
JWT_SECRET="d71Pp3ZytRiPp1nGsaR6WaSZnUGy06kw7A3IpsNZiLE="

# Better-Auth Secret - Generate: openssl rand -base64 32
BETTER_AUTH_SECRET="ZCelo2+f3XwaQd43orGQj3H3sckzuq3te2JGXsmkrSs="

# Better-Auth URL (your API URL)
BETTER_AUTH_URL=http://localhost:3000

# ============================================
# APPLICATION (Required)
# ============================================
# API Server Port
API_PORT=3000

# Web UI Port
WEB_PORT=3001

# CORS Origin (change to your NAS IP or domain)
CORS_ORIGIN=http://localhost:3001

# ============================================
# NEXT.JS PUBLIC VARIABLES (Required)
# ============================================
# These are exposed to the browser (change to your NAS IP or domain)
NEXT_PUBLIC_API_URL=http://localhost:3000

# ============================================
# PRODUCTION / NAS DEPLOYMENT
# ============================================
# Update these when deploying on NAS or production server

# Example 1: Synology/QNAP NAS with Local IP
# GIT_REPO=https://github.com/yourusername/v2-bucket.git
# APP_URL=http://192.168.1.100:3001
# BETTER_AUTH_URL=http://192.168.1.100:3000
# NEXT_PUBLIC_API_URL=http://192.168.1.100:3000
# CORS_ORIGIN=http://192.168.1.100:3001

# Example 2: Production with Custom Domain
# GIT_REPO=git@github.com:yourusername/v2-bucket.git
# APP_URL=https://v2bucket.yourdomain.com
# BETTER_AUTH_URL=https://api.yourdomain.com
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
# CORS_ORIGIN=https://v2bucket.yourdomain.com

# Example 3: Private Repository with Access Token
# GIT_REPO=https://ghp_YourToken123@github.com/yourusername/v2-bucket.git

# ============================================
# SECURITY NOTES
# ============================================
# 1. NEVER commit .env file to git
# 2. Generate strong random passwords:
#    - openssl rand -base64 32
#    - pwgen -s 32 1
# 3. Use different secrets for each environment
# 4. Enable HTTPS in production
# 5. Restrict CORS_ORIGIN in production
# 6. Enable email verification (SMTP) in production
# 7. Regular backup of postgres-data and storage-data volumes
# 8. For private repos, use SSH keys instead of access tokens

# ============================================
# DEPLOYMENT INSTRUCTIONS
# ============================================
# 1. Push your code to GitHub
# 2. Upload docker-compose.yml and .env to your NAS
# 3. Configure this .env file with your settings
# 4. Run: docker-compose up -d
# 5. Docker will automatically clone from Git and build
